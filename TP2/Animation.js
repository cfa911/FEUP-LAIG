class Animation
{
    constructor(id, span)
    {
        this.id = id;
        // duraçao do movimento
        this.span = span;
    }

    update() {
        //calculate matrix
    }

    apply(node) {
        //apply matrix
    }
}