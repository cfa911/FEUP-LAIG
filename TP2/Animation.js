class Animation
{
    constructor(id, span)
    {
        this.id = id;
        // duraçao do movimento
        this.span = span;
    }

    update(deltaTime) {
        //calculate matrix
    }

    apply(deltaTime) {
        //apply matrix
    }
}