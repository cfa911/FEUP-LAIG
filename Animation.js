class Animation
{
    constructor(scene, id)
    {
        this.scene = scene;
        this.id = id;
    }

    update() {
        //calculate matrix
    }

    apply(node) {
        //apply matrix
    }
}